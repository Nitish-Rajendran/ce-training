const message = "Hello from Node.js";
console.log(message);

const fruits = ["apple", "banana", "mango"];
const upper = fruits.map(f => f.toUpperCase());
console.log(upper);

// Browser JS runs inside a browser and can access the DOM.
// Node.js runs outside the browser and is used for backend development.